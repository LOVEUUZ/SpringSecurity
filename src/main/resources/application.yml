spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sg_security?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml   #这个是默认路径，不写也行